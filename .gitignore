# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Local secrets for Cloudflare Pages Functions
.dev.vars

# Legacy reference (not deployed)
_legacy-archive/

# Claude Code IDE local state
.claude/

# Transient cleanup scripts (one-off sweeps)
scripts/brands-*.mjs
scripts/sweep-*.mjs
scripts/convert-*.mjs
scripts/phase-a-*.py
scripts/wave-*-cleanup.py
scripts/wave-5c-outdoor-refactor.py

# Housekeeping
audit-output/
audit-results/
scripts/__pycache__/

# Audit data artifacts and analytical caches (write-once artifacts per methodology.md §7)
scripts/git-mtime-map.json
scripts/*-audit-*/
scripts/audit-*/
# Negate: keep reusable processors visible
!scripts/gsc-wow-*/

# SEO brief: never commit credentials or local outputs
secrets/
.gsc/

# SEO brief output (outputs go to sdar-v2-wiki repo, not here — guard against accidents)
scripts/seo-brief/output/

# Personal source photos used as AI character references. Local only:
# they are Roman's own photos, they are not site assets, and nothing here
# is ever deployed. Only the generated output under public/images/ ships.
reference-photos/
